{
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  },
  "name": "proca-server",
  "license": "AGPL-3.0",
  "scripts": {
    "prepare": "lerna exec yarn prepare",
    "test": "lerna exec --scope @proca/server yarn test"

  }
}
