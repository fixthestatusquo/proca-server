{
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  },
  "name": "proca-server",
  "license": "AGPL-3.0",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "(cd proca; yarn gen.schema) && lerna exec yarn build",
    "test": "lerna exec --scope @proca/server yarn test"

  }
}
