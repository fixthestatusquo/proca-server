{
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.2.4"
  },
  "name": "proca-server",
  "license": "AGPL-3.0",
  "scripts": {
    "install": "lerna bootstrap",
    "prepack": "lerna exec yarn prepack",
    "test": "lerna exec --scope @proca/server yarn test"
  },
  "version": "0.0.3-0"
}
